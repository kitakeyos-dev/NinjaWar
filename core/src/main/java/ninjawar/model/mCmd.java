package ninjawar.model;

public class mCmd {

    public static void printlnRecCommand(int cmd) {
        System.err.println("[REC] " + cmd);
    }

    public static void printlnSendCommand(int cmd) {
        System.err.println("[SEND] " + cmd);
    }
}
